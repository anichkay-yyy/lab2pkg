# Шпаргалка для защиты Лабораторной работы 2

## 🎯 Суть задания

Написать веб-приложение, которое читает **метаданные** (служебную информацию) из графических файлов без использования готовых библиотек для парсинга.

---

## 📋 Что я извлекаю из файлов:

1. **Имя файла** - просто название
2. **Размер изображения** - ширина × высота в пикселях
3. **Разрешение** - DPI (dots per inch) - сколько точек на дюйм при печати
4. **Глубина цвета** - сколько бит на пиксель (определяет количество цветов)
5. **Сжатие** - какой метод сжатия используется
6. **Формат** - тип файла (BMP, PNG, JPEG, etc.)

---

## 🔧 Как это работает?

### Общий принцип:

Каждый графический файл состоит из:
```
[ Заголовок (Header) ] → [ Данные изображения (Image Data) ]
         ↑
  Здесь вся нужная инфа!
```

**Я читаю заголовок файла** → **Извлекаю нужные байты** → **Интерпретирую их**

---

## 📁 Форматы файлов (6 штук)

### 1. BMP (Windows Bitmap) 🖼️

**Самый простой формат!**

#### Структура:
```
Байты 0-1:   "BM" - сигнатура (проверка, что это BMP)
Байты 14-17: Размер DIB заголовка
Байты 18-21: Ширина (int32, Little Endian)
Байты 22-25: Высота (int32, Little Endian)
Байты 28-29: Биты на пиксель (глубина цвета)
Байты 30-33: Метод сжатия (0=нет, 1=RLE8, 2=RLE4)
Байты 38-41: X разрешение (пиксели/метр)
Байты 42-45: Y разрешение (пиксели/метр)
```

#### Откуда что:
- **Размеры**: Байты 18-25, напрямую в пикселях
- **DPI**: Байты 38-45, но в пикселях/метр → конвертирую в DPI: `DPI = пиксели_на_метр × 0.0254`
- **Глубина цвета**: Байт 28-29, напрямую (1, 4, 8, 16, 24, 32 бита)
- **Сжатие**: Байт 30-33 (0=нет, 1=RLE 8-bit, 2=RLE 4-bit)

#### Пример сжатий BMP:
- **0 (BI_RGB)** - без сжатия (самое частое)
- **1 (RLE 8-bit)** - для 8-битных изображений
- **2 (RLE 4-bit)** - для 4-битных изображений

**Особенность**: Если разрешение = 0, значит не указано → ставлю 72 DPI

---

### 2. PNG (Portable Network Graphics) 🎨

**Chunk-based формат** - состоит из блоков (chunks)

#### Структура:
```
Байты 0-7:  PNG Signature (89 50 4E 47 0D 0A 1A 0A)
            ↓
[IHDR chunk] - Image Header (обязательный, первый)
  Байты 16-19: Ширина (uint32, Big Endian)
  Байты 20-23: Высота (uint32, Big Endian)
  Байт 24:     Bit depth (8, 16)
  Байт 25:     Color type (0,2,3,4,6)
  Байт 26:     Compression (всегда 0 = Deflate)
            ↓
[pHYs chunk] - Physical pixel dimensions (ОПЦИОНАЛЬНЫЙ!)
  4 байта: X пиксели/unit
  4 байта: Y пиксели/unit
  1 байт:  Unit (0=unknown, 1=meter)
            ↓
[IDAT chunk] - сжатые данные изображения
            ↓
[IEND chunk] - конец файла
```

#### Откуда что:
- **Размеры**: IHDR chunk, байты 16-23
- **DPI**: pHYs chunk (если есть!), иначе 72 по умолчанию
- **Глубина цвета**: Рассчитываю из bit depth и color type
- **Сжатие**: Всегда "Deflate" (единственный метод в PNG)

#### Color Types (важно для глубины цвета):
- **0** - Grayscale → глубина = bit depth
- **2** - RGB → глубина = bit depth × 3
- **3** - Indexed (палитра) → глубина = bit depth
- **4** - Grayscale + Alpha → глубина = bit depth × 2
- **6** - RGBA → глубина = bit depth × 4

**Пример**: Bit depth = 8, Color type = 6 (RGBA) → Глубина = 8 × 4 = 32 бита

**Особенность**: pHYs chunk может отсутствовать (особенно в скриншотах) → тогда 72 DPI

---

### 3. JPEG (Joint Photographic Experts Group) 📷

**Marker-based формат** - состоит из маркеров (markers)

#### Структура:
```
Байты 0-1: FF D8 - сигнатура JPEG (Start of Image)
           ↓
[SOF marker] - Start of Frame (0xFFC0-0xFFCF)
  Байт +4:   Precision (обычно 8)
  Байт +5-6: Высота
  Байт +7-8: Ширина
  Байт +9:   Количество компонент (3 для RGB)
           ↓
[APP0 marker] - JFIF данные (0xFFE0)
  "JFIF" идентификатор
  Байт +11:    Units (0=нет, 1=DPI, 2=DPC)
  Байт +12-13: X density
  Байт +14-15: Y density
           ↓
[Другие маркеры и данные...]
           ↓
FF D9 - End of Image
```

#### Откуда что:
- **Размеры**: SOF marker (высота и ширина напрямую)
- **DPI**: APP0/JFIF marker, но может быть в разных единицах!
- **Глубина цвета**: precision × components (обычно 8 × 3 = 24 бита)
- **Сжатие**: Всегда "JPEG" (lossy compression)

#### Units в JFIF (важно!):
- **0** - Нет единиц, только aspect ratio → ставлю 72 DPI
- **1** - Уже в DPI → использую напрямую
- **2** - В DPC (dots per cm) → конвертирую: `DPI = DPC × 2.54`

**Особенность**: JPEG всегда с потерями (lossy), в отличие от PNG

---

### 4. GIF (Graphics Interchange Format) 🎬

**Самый простой заголовок!**

#### Структура:
```
Байты 0-5:   "GIF87a" или "GIF89a" - версия
             ↓
[Logical Screen Descriptor]
  Байты 6-7:  Ширина (uint16, Little Endian)
  Байты 8-9:  Высота (uint16, Little Endian)
  Байт 10:    Packed field (флаги + цветовая инфа)
              └─ биты 0-2: размер палитры
              └─ биты 4-6: color resolution
              └─ бит 7: есть ли global color table
```

#### Откуда что:
- **Размеры**: Байты 6-9, напрямую
- **DPI**: **НЕТ в GIF!** → всегда 72 по умолчанию
- **Глубина цвета**: Из packed field (биты 0-2 или 4-6)
- **Сжатие**: Всегда "LZW" (Lempel-Ziv-Welch)

**Особенность**: GIF не хранит DPI, формат для веба

---

### 5. TIFF (Tagged Image File Format) 📄

**Самый сложный!** Поддерживает два порядка байтов (endianness)

#### Структура:
```
Байты 0-1: "II" (Little Endian) или "MM" (Big Endian)
Байты 2-3: 42 (magic number)
Байты 4-7: Offset на IFD (Image File Directory)
           ↓
[IFD] - набор тегов (tags)
  Tag 256 (0x100): ImageWidth
  Tag 257 (0x101): ImageLength (высота)
  Tag 258 (0x102): BitsPerSample
  Tag 259 (0x103): Compression
  Tag 277 (0x115): SamplesPerPixel
  Tag 282 (0x11A): XResolution (RATIONAL тип)
  Tag 283 (0x11B): YResolution (RATIONAL тип)
  Tag 296 (0x128): ResolutionUnit (1=нет, 2=дюймы, 3=см)
```

#### Откуда что:
- **Размеры**: Теги 256 и 257
- **DPI**: Теги 282 и 283 (RATIONAL = числитель/знаменатель), + тег 296 для единиц
- **Глубина цвета**: BitsPerSample × SamplesPerPixel
- **Сжатие**: Тег 259 (много вариантов!)

#### RATIONAL тип:
```
8 байт = 4 байта (числитель) + 4 байта (знаменатель)
Например: 300/1 = 300.0 DPI
```

#### Compression в TIFF:
- **1** - None (без сжатия)
- **5** - LZW
- **6** - JPEG (old-style)
- **7** - JPEG
- **8** - Deflate (как в PNG)
- **32773** - PackBits

#### ResolutionUnit (важно!):
- **1** - Нет единиц
- **2** - Дюймы (inches) → DPI напрямую
- **3** - Сантиметры → конвертирую: `DPI = DPC × 2.54`

**Особенность**: Нужно учитывать byte order (II или MM) при чтении всех чисел!

---

### 6. PCX (PC Paintbrush) 🖌️

**Старый формат, но простой**

#### Структура:
```
Байт 0:     Manufacturer (должен быть 10)
Байт 1:     Version
Байт 2:     Encoding (0=нет, 1=RLE)
Байт 3:     Bits per pixel
Байты 4-5:  Xmin (Left)
Байты 6-7:  Ymin (Top)
Байты 8-9:  Xmax (Right)
Байты 10-11: Ymax (Bottom)
Байты 12-13: Horizontal DPI
Байты 14-15: Vertical DPI
Байт 65:    Number of planes
```

#### Откуда что:
- **Размеры**: (Xmax - Xmin + 1) × (Ymax - Ymin + 1)
- **DPI**: Байты 12-15, **уже в DPI!** (не нужно конвертировать)
- **Глубина цвета**: Bits per pixel × Number of planes
- **Сжатие**: "RLE" если encoding=1, иначе "None"

**Особенность**: Единственный формат, где DPI сразу в правильных единицах!

---

## 🔢 Важные конверсии

### Пиксели/метр → DPI:
```javascript
DPI = pixels_per_meter × 0.0254
```
**Почему**: 1 дюйм = 0.0254 метра (или 1 метр = 39.37 дюймов)

### Dots per cm → DPI:
```javascript
DPI = dots_per_cm × 2.54
```
**Почему**: 1 дюйм = 2.54 см

### Типичные значения DPI:
- **72 DPI** - экран (по умолчанию для веба)
- **96 DPI** - стандарт Windows
- **150 DPI** - среднее качество печати
- **300 DPI** - высокое качество печати
- **600+ DPI** - профессиональная печать

---

## 💾 Byte Order (Порядок байтов)

### Little Endian (LE):
```
Число 1000 (0x000003E8):
Байты: E8 03 00 00
       ↑  младший байт первым
```
**Используется в**: BMP, PCX, TIFF (с "II")

### Big Endian (BE):
```
Число 1000 (0x000003E8):
Байты: 00 00 03 E8
                  ↑  младший байт последним
```
**Используется в**: PNG, JPEG, TIFF (с "MM")

**В коде**:
```javascript
// Little Endian
buffer.readUInt32LE(offset)

// Big Endian
buffer.readUInt32BE(offset)
```

---

## 🎨 Глубина цвета (Color Depth)

### Что это?
Сколько бит используется для кодирования одного пикселя.

### Типичные значения:

| Биты | Цветов | Описание |
|------|--------|----------|
| 1 | 2 | Черно-белое |
| 4 | 16 | 16 цветов |
| 8 | 256 | 256 цветов или Grayscale |
| 16 | 65,536 | High Color |
| 24 | 16.7 млн | True Color (RGB, 8 бит на канал) |
| 32 | 16.7 млн + прозрачность | True Color + Alpha (RGBA) |
| 48 | Много | HDR (16 бит на канал RGB) |

### Как считается:

**Для RGB**:
```
24 бита = 8 бит (Red) + 8 бит (Green) + 8 бит (Blue)
```

**Для RGBA**:
```
32 бита = 8 бит (R) + 8 бит (G) + 8 бит (B) + 8 бит (Alpha)
```

**В BMP**:
```
Глубина = значение из байта 28-29 напрямую
```

**В PNG**:
```
Глубина = Bit Depth × количество каналов
```

**В TIFF**:
```
Глубина = BitsPerSample × SamplesPerPixel
```

---

## 🗜️ Типы сжатия

### Без потерь (Lossless):
- **None** - вообще без сжатия
- **RLE** (Run-Length Encoding) - простое сжатие повторов
- **LZW** (Lempel-Ziv-Welch) - используется в GIF
- **Deflate** - используется в PNG (как в ZIP)
- **PackBits** - вариант RLE для TIFF

### С потерями (Lossy):
- **JPEG** - теряет детали ради меньшего размера

### Как работает RLE (пример):
```
Было:  AAAAAAABBBBBB
Стало: A×7 B×6
```

### Deflate (PNG):
Комбинация двух алгоритмов:
1. **LZ77** - находит повторяющиеся последовательности
2. **Huffman** - кодирует часто встречающиеся данные короче

---

## 🛠️ Моя реализация

### Архитектура:
```
Frontend (React + Next.js)
    ↓ загружает файлы
API Route (/api/analyze-images)
    ↓ передает в парсер
Image Parser (lib/imageParser.ts)
    ↓ определяет формат по расширению
    ↓ вызывает нужный парсер
parseBMP() / parsePNG() / parseJPEG() / ...
    ↓ читает байты напрямую
    ↓ возвращает метаданные
    ↓
Результат в таблице
```

### Что использовал:
- **Node.js Buffer API** - для чтения бинарных данных
- **Ручной парсинг** - все байты читаю сам, без библиотек
- **TypeScript** - для типизации
- **Next.js 14** - фреймворк
- **shadcn/ui** - UI компоненты

### Почему без библиотек?
Чтобы **понять структуру форматов** и научиться работать с бинарными данными.

---

## 🎯 Возможные вопросы на защите

### 1. Откуда берется разрешение в BMP?
**Ответ**: Из байтов 38-45 DIB заголовка, хранится в пикселях/метр, конвертирую в DPI умножением на 0.0254.

### 2. Почему у PNG всегда Deflate?
**Ответ**: Это единственный метод сжатия, определенный в спецификации PNG. Compression method = 0 всегда означает Deflate.

### 3. Что такое Little Endian и Big Endian?
**Ответ**: Порядок байтов при хранении чисел. LE - младший байт первым (BMP, PCX), BE - старший байт первым (PNG, JPEG).

### 4. Как рассчитывается глубина цвета в PNG?
**Ответ**: Зависит от Color Type:
- Grayscale (0) = bit depth
- RGB (2) = bit depth × 3
- RGBA (6) = bit depth × 4
И так далее.

### 5. Зачем нужна информация о DPI?
**Ответ**: Определяет физический размер при печати. Например, изображение 300×300 пикселей при 72 DPI будет 4.2×4.2 дюйма, а при 300 DPI - 1×1 дюйм.

### 6. Почему GIF всегда 72 DPI?
**Ответ**: GIF не хранит информацию о физическом разрешении, формат создан для веба, где это не важно.

### 7. Что такое TIFF IFD?
**Ответ**: Image File Directory - структура из тегов (tags), каждый тег хранит определенную информацию (ширину, высоту, разрешение и т.д.).

### 8. Почему JPEG всегда 24 бита?
**Ответ**: Не всегда! Обычно 24 (8 бит × 3 канала RGB), но может быть 8 (grayscale) или 32 (CMYK для печати).

### 9. В чем разница между RLE и Deflate?
**Ответ**: RLE - простое сжатие повторов. Deflate - сложный алгоритм (LZ77 + Huffman), сжимает лучше.

### 10. Как определяешь формат файла?
**Ответ**: По расширению (.bmp, .png, и т.д.) и верифицирую сигнатурой в заголовке ("BM", PNG signature, 0xFFD8 и т.д.).

---

## 📊 Чек-лист для защиты

✅ Знаю, где хранится каждый параметр в каждом формате
✅ Понимаю разницу между Little/Big Endian
✅ Могу объяснить, как считается глубина цвета
✅ Знаю формулы конверсии (пиксели/метр → DPI)
✅ Понимаю разницу между типами сжатия
✅ Могу объяснить, почему некоторые параметры "по умолчанию"
✅ Знаю особенности каждого формата
✅ Понимаю архитектуру своего приложения

---

## 🚀 Готов к защите!

**Главное**: Понимать, ЧТО я делаю и ПОЧЕМУ именно так. Все остальное - детали, которые можно подсмотреть в коде.

**Совет**: Открой тестовый BMP или PNG файл в hex-редакторе и посмотри на байты - так станет еще понятнее! 👍

